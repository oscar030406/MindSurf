# MindSurf 预训练行动指南 v3（设计与验收版）

本文件是 MindSurf 预训练阶段的**设计与验收标准**。它由总工程师维护，写给执行者
（后续线程、agent、队友）。执行者按本文件做事并提交证据；总工程师只做两件事：
**设计**（定义标准与判据）与**验收**（对照判据检查证据）。本文件不替代
`PROJECT_RULES.md` 的安全、分支与发布约束。

参考指南库为 **`minimind-pretrain-guides`**（`算法/` 44 篇、`ai infra/` 87 篇），
存放在本机共享的 agent 参考库里，不随本仓库分发——它是外部读物，不是本项目产物，本文件各节末尾标注了对应必读篇目。执行者动工前先读对应篇目，再读本节
判据。

---

## 0. 角色与使用规则

**总工程师**：定义质量标准、仪器判据、实验设计；验收里程碑；裁决判据争议。
**执行者**：实现、跑实验、采集证据；不得修改门槛、仪器判据、验收标准——发现判
据本身有缺陷时，提交证据申请修订，由总工程师裁决后修改本文件。
**用户**：持有 M0（目的）与所有人工审核签字权；仪器的一致性审核（§2.3）必须由
用户完成，任何 agent 不得自签。

升级规则：
- 执行者可自决：实现细节、脚本组织、跑批顺序；
- 须报总工程师：任何判据边界情况、任何"仪器可能有问题"的信号、任何多变量申请；
- 须报用户：目的变更、人工审核、不可逆的对外发布、超出既定算力预算的开销。

每个新任务开始：读 `AGENTS.md` → `PROJECT_RULES.md` → 本文件 → 实时核对分支、
服务器、GPU、进程、磁盘、数据资产。每完成一个可验收子项：跑规定验证 → 重开本
文件 → 有证据才改状态 → 确认下一门槛。**没有证据不得标完成；局部完成不得跳过
后续门槛。**

状态词四种：`未开始` / `进行中` / `阻塞`（外部原因明确且替代路径穷尽）/
`完成`（验收判据逐项过且证据可复核）。

---

## 1. 第零层：目的与质量定义

**用户已确认（2026-07-21）：目的是模型质量本身。** 预训练是下游一切（SFT、
对齐、推理）的源头；交付物是当前算力与数据约束下能做到的**最高质量
89,864,448 参数中文为主基座模型**。过程可信是硬约束不是目的：不允许用无效仪器
声称质量，但"测得诚实"不豁免"做得出来"。

质量对 89M 基座模型的可操作定义（四支柱，缺一不可）：

1. **外部锚点不劣化**：上游 MiniMind 官方发布的预训练 checkpoint（与本项目共用
   同一 6,400 词表 tokenizer；26M 与 104M 两档）用本项目仪器、本项目 holdout
   同条件评测。质量声明形如"89M 候选显著优于官方 26M、不劣于官方 104M"——它不
   依赖任何自家门限，是本项目唯一非循环的质量标尺。锚点评测前必须核对
   tokenizer 摘要一致，否则 loss 不可比。
2. **跨源泛化**：Team holdout（`wyywnab` 源，与 Official 异源）上的 loss 同步
   改善。同分布 holdout loss 只度量对自家语料的压缩，不构成质量声明。
3. **生成健康**：经效度合格仪器（§2）判定的固定长度贪心生成不退化。
4. **下游可用性**：由 SFT 阶段回流验证，本阶段不自证、不虚报。

非目标（明示，防止执行者跑偏）：公开榜名次、指令遵循能力、任何 SFT 后才有意义
的指标。

> 参考：`算法/05_训练阶段与数据/预训练（Pretrain）详解.md`、
> `ai infra/09_Comparison/训练 Infra vs 推理 Infra.md`

---

## 2. 第一层：仪器效度

**任何度量参与门控决策之前，必须通过本节四问并在效度台账留证。未通过者可记录、
可报告，不得判定通过或失败。** 本层是 v1→v3 演进的核心：v1 要求阈值有出处，
不要求度量有效度，结果一天之内三个仪器被实测证明量的不是它声称的东西（长度伪装
成质量两次、早停伪装成健康一次）。

### 2.1 不变性——干预式测试为准

列出仪器**不应**依赖的因素（至少：文本长度、书写系统、是否早停、格式噪声），
逐项做干预测试：固定生成长度重测（消早停）、等长截断重测（消长度）、折叠空白
重测（消格式）、分语种分组重测（消书写系统）。干预后差异消失 = 伪信号，不合格。

观察性相关只作筛查线索。已有反例：`generation.repetition` 与长度相关 0.786，
但人工查验证实被判重复的完成确是逐句循环的真退化——相关是混淆（退化模型既重复
又不停），按"相关即不合格"会错杀方向正确的度量。

**已知失败分类表**（执行者建新仪器前对照自查）：

| 失败模式 | 症状 | 实例 |
| --- | --- | --- |
| 长度代理 | 与长度强相关，被丢样本显著更长/更短 | 重复率 0.5 阈值：丢 11.6%，被丢中位 729 字符 |
| 书写系统代理 | 分语种丢弃率差一个数量级 | bigram 多样性：英文丢 6.07%、中文 0.00% |
| 早停免检 | 输出越短得分越好 | 7 字符完成得满分 0.000 |
| 模板繁殖 | 去重后有效样本远小于名义样本 | MCQ v2：32 条 long_context 实为 4 模板×8 |

### 2.2 区分度——棘轮 + 金丝雀，禁止描点

- 失去区分度的判据（余量 > 10×噪声底，暂定值）**退役为金丝雀**：保留宽松绝对值
  作灾难回归报警（如 `strict_val < 2.42`），不再区分候选，不删除。
- 门控判据用**棘轮**：不得比参照差超过 k×噪声底（k 暂定 3）。参照系 = **外部
  锚点与历史最优候选取严**。
- **禁止照当前最好模型的实测值描新绝对阈值**——那是 v1 原罪（门限描自一个欠训
  练参考模型，被甩开 1.24 nats 后八项判据全部失效）的复刻。

### 2.3 一致性——人工对照，用户签字

分层抽样 + 用户盲审 + 结论落档 + 审计绑定被审版本摘要（`reviewed_this_benchmark`
必须为真）。布尔开关声称"已人工审核"是本项目发生过的真实造假路径，已被制度性
堵死：审核声明只能指向真实审核文档及其 SHA-256。

### 2.4 噪声底——两种来源分开报

- 评测集抽样噪声：单 run 内 bootstrap（优先；MCQ 已有配对 ±7pp）；
- 训练随机性：换 seed 重跑，受算力约束 seed 封顶 2，n=2 差值只是**下界**，结论
  强度相应降级。

效应量报告固定格式：`效应量 ± 噪声底`，结论只能是**改善 / 劣化 / 无法区分**。
落在噪声底内的差异禁止报方向。实测参考（60k 步双 seed）：strict loss
0.0024–0.0068，domain 0.0005–0.0144，MCQ ±7pp，repetition ±2/10。

### 2.5 样本量下限

仪器样本量必须支撑判定粒度。反例存档：退化探针 n=10，门在 5 对 4 之间翻转，
二项噪声 ±2 即掷硬币——度量定义再完美也无效。生成探针 ≥100 条（v3 探针集已
备，100 条独立主题、零模板繁殖）；MCQ 192 题极限 ±7pp 而关心效应 3–5pp，
**物理上分不出**——扩到 ~1000 题或永久降级 smoke，动工前强制功效分析。

### 2.6 效度台账

集中一处，字段：仪器名、声称度量、不变性干预结果、区分度余量、一致性证据、
噪声底、样本量、**门控资格**、复核日期、**原始测量产物链接**（只有结论不算）。
缺项 = 无资格。效度检查脚本自身带测试入库——验证器也是代码，同一天曾产出三个
带偏见的过滤器。

> 参考：`ai infra/02_Training_Infra/训练监控.md`（告警设计反面：单阈值误报）、
> `ai infra/01_Concepts/MFU.md`

---

## 3. 第二层：实验纪律

### 3.1 一臂一变量，按三元组机械强制

臂相对其**声明对照**的 `(源码 git SHA, 解析后配置, 数据身份)` 三元组实质差异
必须恰好一处（run 名、输出目录、日志频率不计）。多于一处拒绝启动，除非显式
`--multi-variable` 且冻结规格写明理由。只比配置不够——促成本条的事故里 betas
与 RMSNorm 修复是纯代码改动，配置 diff 全盲。自觉无效有实证：同一执行者写下
"两个变量同时变了无法归因"后照跑不误。**必须让工具拦。**

### 3.2 冻结在前

假设、触发条件、停止条件在占 GPU 前入库。事后调触发条件 = 没有触发条件。

### 3.3 受混淆结论显式标注失效

在原文档标注原因，不得静默重释。现行失效清单见 §9。

### 3.4 归因优先于加预算

失败先归因。禁止用加步数掩盖未归因的失败。

### 3.5 破坏性语料操作先标定后执行、抽检留档

任何不可逆删除（去重/过滤/PII）：阈值先按 §2.1 在 ≥50k 真实样本上做不变性
标定；执行时随机抽 ≥50 条被删 + ≥50 条保留存档供人工复查。三个带偏见过滤器
全部靠"先测量、再看被丢样本"抓出，任何一个直接上线都会定向削薄语料且总量上
看不出来。

### 3.6 资源是设计输入

共享 GPU、单臂 2.5–7 小时、有截止日。每轮实验前声明墙钟成本与其购买的信息量；
买不到新信息的实验不跑；大改动先做成本-功效分析。

> 参考：`ai infra/02_Training_Infra/训练实验管理.md`、`训练 Infra 总览.md`

---

## 4. 第三层：数据管线标准

阶段顺序固定：原始文本 → 清洗 → 质量过滤 → 安全/PII → **近似去重** → 打散 →
预分词 → 流式加载。每阶段产物有独立身份（SHA-256 + manifest 继承链），输入
视图不动，输出为新身份——保证任意两阶段可对照。

各阶段验收判据：

| 阶段 | 判据 | 现状证据 |
| --- | --- | --- |
| 清洗/归一 | manifest 如实声明作用域（NFKC 只用于摘要时不得声称语料被归一化） | 已修正一次虚假声明 |
| 质量过滤 | 阈值过 §2.1 标定；分语种丢弃率差 < 3×；被丢中位长度 ≤ 保留中位 | zh 0.05% / other 0.14%，被丢中位 131 对保留 295 |
| PII | 身份证校验位、银行卡 Luhn 验证，防误杀；计数入报告 | 全量 8,739 行 |
| 近似去重 | MinHash 字符 shingle（中文无分词依赖），阈值几何写明检出概率 | 全量 444,897 条（5.26%） |
| 打散 | **全局置换**，窗口打散不接受（修不了源排序；两语料尾部均为纯英文/代码，部分 epoch 顺序读必有偏） | 已实测证伪顺序读 |
| 预分词 | memmap 块与内联打包**逐位一致**（含恢复与跨 epoch），uint16 上限对词表校验 | 等价性测试 5 项全过；生产实测 7.43 it/s 对 6.75–7.07 |

> 参考：`ai infra/02_Training_Infra/数据管线.md`、
> `算法/05_训练阶段与数据/Dataset 与 DataLoader 详解.md`

---

## 5. 第四层：训练与算法标准

**配方基线**（已按参考库审计修正，执行者不得回退）：AdamW betas `(0.9, 0.95)`
且可配置；RMSNorm eps `1e-6`、均方归约上溯 float32、eps>0 有校验；梯度累积
loss 记窗口均值且除以 K；裁剪顺序 `unscale_ → clip → step`；WSD 调度
（warmup 200 / stable 0.8 / min_lr_ratio 0.1）。**注意**：改 `max_steps` 即改
整条 LR 轨迹形状，这是"预算"的题中之义，比较时必须写明。

**预算标尺**：计算最优约 20 tokens/参数。父模型 1.37（严重欠训）；60k 步 =
8.2；filtered 全量单 epoch = 23.6（当前 172k 臂）。

**Checkpoint 契约**：原子写（临时名 + fsync + rename）；内容含模型、optimizer、
scheduler、AMP、CPU/CUDA/Python/numpy RNG、global/micro step、packed-block
游标、seen tokens；恢复逐位一致（SIGTERM 与 SIGKILL 注入均已验证）；
`keep_last ≥ 3`；best 独立跟踪。

**监控四层**：算法（loss、ppl、grad norm、LR）/ 计算（tokens/s、**训练中在线
MFU**、显存）/ 硬件（温度持续超限、功耗、**任一不可纠正 ECC 即 critical**）/
数据（等待时长）。告警用趋势与联合条件，不用单点阈值（单 micro-batch loss 在
1.5–4.7 间无故障波动，单点必误报）。MFU 基线 28.7–31.3%，该规模属正常，吞吐
不是瓶颈，不再投入。

**血缘**：run 记录含 git commit、依赖摘要、torch/CUDA、GPU 型号、确定性开关、
数据身份、配置指纹；候选注册绑定 preflight（源码 SHA）与训练摘要。run 名复用
冲突必须可见（config 指纹）。

> 参考：`ai infra/02_Training_Infra/Checkpoint.md`、`训练容错.md`、
> `训练监控.md`、`GPU 利用率.md`；
> `算法/04_训练稳定性与优化器/Optimizer 优化器详解（Adam & Muon）.md`、
> `Epsilon 值对训练的影响.md`

---

## 6. 第五层：评测与门

门 v3 结构 = **棘轮**（对外部锚点与历史最优取严，容差 3×噪声底）+ **金丝雀**
（退役绝对值抓灾难回归）+ **生成健康**（固定长度贪心、n≥100、按 §2 验证后的
判据）。同分布 holdout 与六 domain 切片继续测量并报告，但只作金丝雀。

外部锚点协议：拉取上游官方 26M / 104M 预训练 checkpoint → 核对 tokenizer
SHA-256 → 用本项目评测管线出全套指标 → 入效度台账作棘轮参照。锚点架构与本项目
不同（这是外部锚点的本义），只比同 tokenizer 下的 loss 与生成健康，不比参数
效率之外的结构性指标。

MCQ 政策：v2 永久降级 smoke（用户审核结论：不可作能力证据）。v3 动工前提交
功效分析给总工程师，算不过账就不建。

> 参考：`ai infra/01_Concepts/Latency 与 Throughput.md`（M6 用）、
> `算法/03_位置编码与长上下文/外推性 Benchmark.md`（评测设计反面教材对照）

---

## 7. 里程碑：交付物与验收判据

**M0 目的确认 — `完成`**（2026-07-21 用户确认：模型质量本身）。

**M1 仪器效度与外部锚点 — `完成`（锚点降级，理由入档）**（2026-07-22）
交付物：修正后的生成健康仪器（固定长度 + v3 探针 100 条，6 项测试）；门 v3
配置与判定脚本（9 项测试）；外部锚点端口与 tokenizer 闸门（9 项测试）+
作废证据 `artifacts/evaluation/anchor_upstream_void.json`。
验收：生成仪器四项干预测试全过；门 v3 对两个 checkpoint 出过判定且可复算；
**锚点 tokenizer 摘要核对记录在案且判定为不匹配** —— 上游已发布权重训练所用
tokenizer 为 `d98595c6…`，本项目为 `71f32c68…`，同为 6,400 词表但 1,772 个
token 互不存在。用本项目 tokenizer 评测锚点得 12.5733 / 12.0227 nats，而均匀
猜测为 8.7641，比随机还差 = token id 含义错位。模型端口本身逐位忠实（对上游
自己的模型代码最大绝对 logit 差 0.0），**作废原因单独落在 tokenizer 条件上**。
按 §1"不可行就如实降级、不硬凑"，**外部锚点标注未成立，四支柱降为三支柱**，
不报告任何方向的比较。详见 `docs/experiments/2026-07-22-external-anchor.md`。

**M2 数据与评测接口 — `完成`**（统一索引、身份校验、配置回归测试在案）。

**M3 产出并确认质量候选 — `完成`**（2026-07-22）
交付物：两个候选
`minimind_official_full_filtered_v1_budget172k_seed{20260511,20260721}_80m_step168000`，
已登记于 `artifacts/model_registry.json`，`gate_source: "verdict"`。
验收：门 v3 判定 `passed: true` 且可复算，八项门控判据余量 +0.10~+0.17 nats；
跨源复核用**与棘轮同一参照**的 shuf60k seed20260721：Team val 3.1927 → 3.0550、
test 3.2170 → 3.0765，效应 −0.135~−0.141 ± 双 seed 差 0.0030–0.0045（下界），
方向可报 = **跨源改善**；生成健康 100 探针零退化。
**支柱达成：3 / 4**（外部锚点未成立见 M1；下游可用性留待 SFT）。
双 seed **只扰动初始化**这一限定已随注册记录登记（`--limitation`），未省略。

**M4 资产耐久性与干净环境复现 — `完成`**（2026-07-22，用户变更目标）
用户 2026-07-22 决定：**不再向备份服务器备份，资产收到本机项目目录**。
验收：本机清单 `artifacts/infra/local_asset_inventory.json`，
**17 项资产 2.88 GB，missing 0，failed 0**，含两份候选 checkpoint（sha256 与
注册表逐字节相符）、训练摘要、preflight、评测与判定产物、注册表、数据视图
manifest、tokenizer、两份发布权重。`backup_assets.py` 新增 `--in-place`，
并把耐久性声明缩到它实际证明的范围（同机记录 ≠ 第二份副本）。
服务器工作区收敛问题随磁盘清理一并消解。

**M5 训练 Infra 闭环 — `完成`**（主机重启子项 `阻塞`：无权限，SIGKILL 替代
已穷尽并通过 10 项一致性检查）。

**M6 推理 Infra — `未开始`，本阶段不做**（触发条件：M3 有交付候选且用户启动
部署需求）。

**M7 发布收口 — `完成`**（2026-07-22）
交付物：README 已改写为最终事实（不再描述 10,000 步对照）；发布说明
`docs/releases/2026-07-22-pretrain-80m.md` 区分数据源与许可，明示 CC-BY-NC-4.0
经权重传导且随每个衍生物继承；许可授权按事实记录为
`contribution_permitted_by_upstream_author` + `authorization` 块（证据为维护者
口头陈述，**未写成"已核实"**）；五向追溯表在发布说明 §7。
验收：`release_gate.py --range upstream/pretrain...HEAD` 通过（104 文件）；
169 项测试通过、覆盖率 83.28%；发布 PR io-wy/MindSurf#8；权重经 GitHub Release
发布（1.08 GB checkpoint 不进 Git，改发 359 MB 纯权重导出件）。
**任何能力性表述均追溯到有门控资格的仪器**：MCQ 与旧 repetition 在 README 与
发布说明中均显式标注无资格，不支撑任何强弱结论。

---

## 8. 暂不建设（挂触发条件）

DDP/FSDP、集群调度、vLLM/TensorRT-LLM、量化、推测解码、多副本路由、多 rank
数据分片（预分词 memmap + 索引已是单机等价物）。触发条件：单卡放不下，或多卡
可显著缩短受约束的关键路径，或 M6 启动。89.9M 单卡模型下均为纯负债。

> 参考：`ai infra/04_Systems/*`、`09_Comparison/*`（届时选型用）

---

## 9. 当前事实基线（2026-07-21）

**成立**：预算是历史主要瓶颈（1.37 t/p 起步，10k→60k 步 strict mean
2.3725→1.9492）；两语料均按来源排序；固定 60k 预算下全量对 mini **无法区分**
（0.008–0.012 nats，噪声底内）；数据管线四能力建成且阈值经真实标定（全量去重
5.26%、PII 8,739 行、总丢弃 5.44%；预分词 5,500,556 块 = 2.117B tokens）。

**失效**（原因已在原文档标注）："targeted 数据有害"（混淆）；全量 60k 两臂
（有序 0.34 epoch = 只见文件前 34%）；已登记候选的"第二 seed 确认"（只扰动
初始化）；八项 loss 判据（失去区分度，退役金丝雀）；repetition 旧仪器（早停
免检 + n=10）与 MCQ v2（用户判定不一致 + 功效不足）作为能力判据。

**历史候选处置**：`minimind_official_v1_budget60k_seed{20260511,20260721}-80m`
通过当时的门属实，但训练于缺陷代码（betas 0.999、fp16 RMSNorm）与有序语料，
当前代码不可复现。定性：**历史候选，血缘不完整，不作最终交付**。

**在跑**：无。两台服务器均无本项目训练进程。

**关键资产（2026-07-22 更新）**：候选权重、评测与判定产物、注册表、tokenizer、
holdout、数据 manifest 全部收在**本机项目目录**，
`artifacts/infra/local_asset_inventory.json` 记 17 项 2.88 GB，missing 0、
failed 0。两台服务器按用户指示已清理数据与权重（共释放约 105 GB），只留 git
检出与 `~/.venvs/mindsurf-cu124`。

**因清理而不再存在的**（结论已入档，故可删）：过滤+预分词视图（4.24 GB memmap
+ 8.2 GB JSONL）——可按 `gongjy/minimind_dataset@74aad49f` 与
`artifacts/data/minimind_official_full_filtered_v1/*.json` 的 SHA-256 重建并校验；
门 v3 参照 checkpoint（shuf60k seed20260721 `final_model.pt`）——**棘轮读的是它的
评测 JSON `reeval_shuf60k_seed20260721_80m.json`，该 JSON 已保留**，参照系不受影响；
上游锚点 checkpoint——可按 §M1 记录的 sha256 从 ModelScope 重新拉取。

**新增的机械强制**：`scripts/release_gate.py` 的 `--range` 模式现在**同时审计
提交消息**。此前它只查文件内容，工具协作尾注因此整批通过并被推到共享分支，
返工重写了 79 个提交的消息（树内容逐位未变）。规则若没有工具兜底，就是没有规则
——这与 §3.1 三元组强制"必须让工具拦"同一条道理。
